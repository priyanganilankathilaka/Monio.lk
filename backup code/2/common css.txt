common css


@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Sinhala:wght@300;400;500;600;700;800&family=Noto+Serif+Sinhala:wght@300;400;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap');

:root {
  --bg-main: #f8fafc;
  --card-bg: #ffffff;
  --border-color: #e2e8f0;
  --text-dark: #0f172a;
  --text-muted: #64748b;

  --royal-blue: #1e40af;
  --bright-blue: #2563eb;
  --emerald-green: #059669;
  --coral-red: #e11d48;
  --amber-gold: #d97706;

  --shadow-sm: 0 4px 12px rgba(15, 23, 42, 0.03);
  --shadow-md: 0 10px 30px rgba(15, 23, 42, 0.06);
  --shadow-lg: 0 20px 40px rgba(15, 23, 42, 0.08);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background-color: var(--bg-main);
  color: var(--text-dark);
  font-family: 'Noto Sans Sinhala', 'Plus Jakarta Sans', sans-serif;
  overflow-x: hidden;
  line-height: 1.7;
  width: 100%;
  -webkit-font-smoothing: antialiased;
}

.main-container {
  max-width: 1200px;
  width: 100%;
  margin: 0 auto !important;
  padding: 0 24px;
}

h1, h2, h3 { font-family: 'Noto Serif Sinhala', serif; }

.top-ribbon {
  width: 100%;
  background: linear-gradient(90deg, #1e40af, #2563eb);
  color: #ffffff;
  padding: 10px 20px;
  text-align: center;
  font-size: 13px;
  font-weight: 600;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 12px;
  box-shadow: var(--shadow-sm);
  position: relative;
  z-index: 101;
}

.ribbon-badge {
  background: #e11d48;
  color: #ffffff;
  padding: 2px 10px;
  border-radius: 20px;
  font-size: 10px;
  font-weight: 800;
}

.ribbon-link { color: #fde047; text-decoration: none; font-weight: 700; }
.ribbon-link:hover { text-decoration: underline; }

.navbar-wrapper {
  width: 100%;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid rgba(226, 232, 240, 0.8);
  box-shadow: var(--shadow-sm);
  position: sticky;
  top: 0;
  z-index: 100;
  transition: all 0.3s ease;
}

.navbar-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 16px 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.main-logo { height: 40px; width: auto; object-fit: contain; }

.nav-links a {
  color: var(--text-muted);
  text-decoration: none;
  margin: 0 18px;
  font-weight: 600;
  font-size: 15px;
  transition: all 0.25s ease;
}

.nav-links a:hover, .nav-links a.active { color: var(--bright-blue); }
.highlight-link { color: var(--amber-gold) !important; font-weight: 700 !important; }

.cta-btn-nav {
  background: var(--bright-blue);
  color: #ffffff;
  padding: 10px 24px;
  border: none;
  border-radius: 30px;
  font-weight: 700;
  cursor: pointer;
  box-shadow: 0 4px 14px rgba(37, 99, 235, 0.25);
  transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.cta-btn-nav:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(37, 99, 235, 0.35);
}