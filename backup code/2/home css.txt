home css


/* ==========================================================================
   MAIN HOME PAGE (home.css) - ULTRA-MODERN MASTER EDITION
   ========================================================================== */

@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Sinhala:wght@300;400;500;600;700;800;900&family=Noto+Serif+Sinhala:wght@400;600;700;800&family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap');

:root {
  /* Dynamic Palette */
  --bg-home: #fcfbf8;
  --surface-card: rgba(255, 255, 255, 0.85);
  --border-light: rgba(226, 232, 240, 0.7);
  --border-gold: rgba(245, 158, 11, 0.3);

  --text-main: #0f172a;
  --text-sub: #475569;
  --text-muted: #64748b;

  --accent-gold: #d97706;
  --grad-gold: linear-gradient(135deg, #f59e0b 0%, #d97706 50%, #b45309 100%);
  --grad-dark: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);

  /* Advanced Shadows */
  --shadow-subtle: 0 4px 16px -2px rgba(15, 23, 42, 0.04);
  --shadow-hover: 0 20px 40px -10px rgba(15, 23, 42, 0.08);
  --shadow-gold-glow: 0 16px 36px -8px rgba(217, 119, 6, 0.22);

  --ease-spring: cubic-bezier(0.175, 0.885, 0.32, 1.275);
  --ease-smooth: cubic-bezier(0.16, 1, 0.3, 1);
}

/* Global Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body.home-page-body {
  background-color: var(--bg-home);
  background-image: 
    radial-gradient(circle at 10% 10%, rgba(254, 243, 199, 0.4) 0%, transparent 45%),
    radial-gradient(circle at 90% 80%, rgba(224, 242, 254, 0.35) 0%, transparent 50%);
  background-attachment: fixed;
  color: var(--text-main);
  font-family: 'Plus Jakarta Sans', 'Noto Sans Sinhala', sans-serif;
  line-height: 1.8;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
}

.home-container {
  max-width: 1200px;
  width: 100%;
  margin: 0 auto;
  padding: 0 24px;
}

/* -------------------------------------------------------------------------- */
/* 1. HERO SECTION WITH GLASS CARD FLOATING                                   */
/* -------------------------------------------------------------------------- */
.home-hero {
  padding: 90px 0 60px 0;
  text-align: center;
  position: relative;
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 6px 20px;
  background: linear-gradient(135deg, rgba(254, 243, 199, 0.9), rgba(253, 230, 138, 0.5));
  color: #92400e;
  border-radius: 100px;
  font-size: 0.8rem;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  margin-bottom: 24px;
  border: 1px solid var(--border-gold);
  box-shadow: 0 4px 14px rgba(245, 158, 11, 0.12);
}

.home-hero h1 {
  font-family: 'Noto Serif Sinhala', serif;
  font-size: 3.2rem;
  font-weight: 800;
  line-height: 1.25;
  margin-bottom: 20px;
  letter-spacing: -1px;
}

.home-hero h1 span {
  background: var(--grad-gold);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.home-hero p {
  max-width: 720px;
  margin: 0 auto 36px auto;
  font-size: 1.15rem;
  color: var(--text-sub);
}

/* Hero CTA Buttons */
.hero-cta-group {
  display: flex;
  justify-content: center;
  gap: 18px;
  margin-bottom: 50px;
}

.btn-primary-glow {
  padding: 14px 32px;
  background: var(--grad-gold);
  color: #ffffff;
  font-weight: 700;
  font-size: 1rem;
  border-radius: 14px;
  text-decoration: none;
  box-shadow: var(--shadow-gold-glow);
  transition: all 0.35s var(--ease-smooth);
  display: inline-flex;
  align-items: center;
  gap: 10px;
}

.btn-primary-glow:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 22px 45px -8px rgba(217, 119, 6, 0.35);
}

.btn-secondary-glass {
  padding: 14px 32px;
  background: var(--surface-card);
  backdrop-filter: blur(12px);
  color: var(--text-main);
  font-weight: 700;
  font-size: 1rem;
  border-radius: 14px;
  text-decoration: none;
  border: 1px solid var(--border-light);
  box-shadow: var(--shadow-subtle);
  transition: all 0.35s var(--ease-smooth);
}

.btn-secondary-glass:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
  border-color: var(--accent-gold);
}

/* -------------------------------------------------------------------------- */
/* 2. FEATURE / CATEGORY CARDS GRID                                         */
/* -------------------------------------------------------------------------- */
.features-section {
  padding: 60px 0;
}

.feature-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
  margin-top: 40px;
}

.feature-card {
  padding: 36px 28px;
  background: var(--surface-card);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--border-light);
  border-radius: 24px;
  box-shadow: var(--shadow-subtle);
  transition: all 0.4s var(--ease-smooth);
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: transparent;
  transition: background 0.3s ease;
}

.feature-card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-hover);
  border-color: rgba(217, 119, 6, 0.3);
  background: #ffffff;
}

.feature-card:hover::before {
  background: var(--grad-gold);
}

.feature-icon-wrapper {
  width: 58px;
  height: 58px;
  background: linear-gradient(135deg, #fffdf5 0%, #fef3c7 100%);
  border: 1px solid var(--border-gold);
  border-radius: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.6rem;
  margin-bottom: 20px;
  box-shadow: 0 4px 12px rgba(245, 158, 11, 0.08);
}

.feature-card h3 {
  font-family: 'Noto Serif Sinhala', serif;
  font-size: 1.35rem;
  margin-bottom: 12px;
  color: var(--text-main);
}

.feature-card p {
  font-size: 0.95rem;
  color: var(--text-sub);
  margin-bottom: 20px;
  flex-grow: 1;
}

.card-link {
  font-weight: 700;
  font-size: 0.9rem;
  color: var(--accent-gold);
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  transition: gap 0.25s ease;
}

.card-link:hover {
  gap: 10px;
}

/* -------------------------------------------------------------------------- */
/* 3. STATS & HIGHLIGHT BAR                                                 */
/* -------------------------------------------------------------------------- */
.stats-banner {
  margin: 60px 0;
  padding: 40px;
  background: var(--grad-dark);
  border-radius: 28px;
  color: #ffffff;
  box-shadow: var(--shadow-hover);
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  text-align: center;
}

.stat-item h4 {
  font-size: 2.2rem;
  font-weight: 800;
  color: #fef3c7;
  font-family: 'Plus Jakarta Sans', sans-serif;
}

.stat-item p {
  color: #94a3b8;
  font-size: 0.88rem;
  font-weight: 500;
}

/* -------------------------------------------------------------------------- */
/* 4. RESPONSIVE DESIGN                                                      */
/* -------------------------------------------------------------------------- */
@media (max-width: 1024px) {
  .feature-grid { grid-template-columns: repeat(2, 1fr); }
  .stats-banner { grid-template-columns: repeat(2, 1fr); gap: 30px; }
  .home-hero h1 { font-size: 2.5rem; }
}

@media (max-width: 640px) {
  .feature-grid, .stats-banner { grid-template-columns: 1fr; }
  .hero-cta-group { flex-direction: column; }
  .home-hero h1 { font-size: 2rem; }
}